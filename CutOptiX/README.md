
  # Dashboard UI for CutOptix

  This is a code bundle for Dashboard UI for CutOptix. The original project is available at https://www.figma.com/design/3WkROeuI0xgrjm0O04vtAw/Dashboard-UI-for-CutOptix.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  